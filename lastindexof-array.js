// lastIndexOf  = Returns the index of the last occurrence of a specified value in an array. 

let elements =  [0,1,2,3,4,5, 'element', 'element', true];

let method = elements.lastIndexOf('element')

console.log(method);        // 7
console.log(elements);      // [ 0, 1, 2, 3, 4, 5, 'element', 'element', true ]