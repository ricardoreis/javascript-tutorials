//indexOf = retorna o índice do primeiro elemento 

let elements =  [0,1,2,3,4,5, 'element', true];

let method = elements.indexOf('element')

console.log(method);        // 6
console.log(elements);      // [ 0, 1, 2, 3, 4, 5, 'element', true ]
