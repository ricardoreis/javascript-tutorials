

// push         = insere um elemento e retorna o length do array
// pop          = remove o último elemento e retorna o elemento removido
// shift        = remove o primeiro elemento de um array e retorna o elemento removido;
// splice       = remove elementos, pode substituir elementos e retorna o array removido
// slice        = retorna uma fatia de um array
// indexOf      = retorna o índice do primeiro elemento especificado
// lastIndexOf  = retorna o índice do último elemento especificado
// findIndex    = passando uma função, retorna o índice do priemiro elemento ou -1
// every        = determina se todos os elementos satisfazerm o teste especificado, retorna true ou false
// find         = retorna o valor do primeiro elemento caso a condição resultar true
// some         = retorna true ou false dependendo do resultado da função de callback
// sort         = ordenar os elementos
// fill         = preenche os elemtentos com um valor especificado 
// filter       = retorna um array com os elementos que atendem a uma condição especificada dentro de callback
// map          = transforma os elementos conforne a função de callback 
// forEach      = executa uma função especificada para cada elemento dentro do array
// reduce       = reduz o array a um valor
// reduce       = reduz todos os elementos de um array a um único valor



let elements =  [0,1,2,3,4,5, 'element', 'string element', true];

let method = elements.map

console.log(method);
console.log(elements);


